# CUDA-only additions for the Linux/NVIDIA profile: OmniVoice speech and its
# torch stack. Compiled together with runtime.in into cuda-linux-x86_64.lock.
accelerate==1.14.0
omnivoice==0.2.1
torch==2.13.0
torchaudio==2.11.0
transformers==5.15.1
